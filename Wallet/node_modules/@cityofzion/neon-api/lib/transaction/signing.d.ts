import { wallet, tx } from "@cityofzion/neon-core";
export declare type SigningFunction = (tx: tx.Transaction, details: {
    network: number;
    witnessIndex: number;
}) => Promise<string>;
export declare function signWithAccount(acct: wallet.Account): SigningFunction;
//# sourceMappingURL=signing.d.ts.map